return {
    version = "1.2",
    flex_mode = true,  -- set to false for normal, true for dev/flex
    last_update_time = os.time() - (3*24*60*60), -- simulate last update was 3 days ago
    needs_update = false
}
